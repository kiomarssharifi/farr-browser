#!/usr/bin/env node
'use strict';
require('../lib/cli').main(process.argv.slice(2)).then(
  (code) => { process.exitCode = code; },
  (e) => { process.stderr.write(`error: ${e.message}\n`); process.exitCode = 1; },
);
